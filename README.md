# reger.drnh.fr

A private gift site. All meaningful content — text, images, the voucher —
is AES-256-GCM encrypted inside `index.html` and only decrypts client-side
with a key that is not in this repository.
